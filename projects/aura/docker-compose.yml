services:
  aura:
    build: .
    image: aura-agent:latest
    container_name: aura-intelligent-agent
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./data:/app/data
    network_mode: host